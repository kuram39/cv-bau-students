# Floor must include the `thinking=` parameter on Messages.create — the
# translator + reasoning calls pass thinking={"type":"adaptive"}; older SDKs
# (e.g. 0.40) reject the kwarg with TypeError before reaching the API.
anthropic>=0.101.0
streamlit>=1.32.0
pydantic>=2.5.0
sqlalchemy>=2.0.0
pypdf>=4.2.0
python-docx>=1.1.0
plotly>=5.18.0
pandas>=2.1.0
python-dotenv>=1.0.0
rapidfuzz>=3.0.0
# Install the local cv_bau_students package itself. Streamlit Cloud only
# runs `pip install -r requirements.txt`; without `.` the imports fail
# at startup. Same lesson as cv-estimator.
.
