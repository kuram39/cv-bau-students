# PR Review — 2026-07-01

Auto-generated alignment check. Covers **PRs #21–#51** (merged since last
review `docs/PR_REVIEW_2026-06-01.md`, which covered #7–#20). All 27 PRs
below merged within a 30-hour window (2026-06-01T08:01 → 2026-06-02T14:14);
**nothing has merged in the ~29 days since.** Also notes a stale open PR
stack (#39–#42) that implements a CLAUDE.md-deferred item but has sat
unmerged for a month.

---

## Merged PRs since last check (#21–#51)

| # | Title | Type | Status |
|---|---|---|---|
| #21 | Guard `--truncate` against wiping candidate data (R6) | fix | ✅ merged |
| #22 | PR review 2026-06-01 + stale-annotation cleanup + thinking default ON (F1) | docs+fix | ✅ merged |
| #23 | Rebake `seed.sqlite.gz` with post-MVP demo | chore | ✅ merged |
| #24 | Role-scoped seed — shrink DB ~200k→~2k rows | perf | ✅ merged |
| #25 | Recompute target skills only on Save (`st.form`) | fix(ui) | ✅ merged |
| #26 | Postgres seq reset (uploads) + show position detail to candidate | fix | ✅ merged |
| #27 | `record_interest` dialect-agnostic upsert (Postgres) | fix | ✅ merged |
| #28 | Evidence-strength + transparency + bias hardening (MODEL_CARD.md) | feat | ✅ merged |
| #29 | Skip scraped-ad overlay when `job_ads` already seeded | fix(bootstrap) | ✅ merged |
| #30 | Classify candidate type relative to the target ad | feat(detector) | ✅ merged |
| #31 | No/low real experience → student, not experienced | fix(detector) | ✅ merged |
| #32 | Rewrite README to current state | docs | ✅ merged |
| #33 | Codex P2 edge-cases in evidence + classifier | fix | ✅ merged |
| #34 | Rebake `seed.sqlite.gz` from fresh reseed | chore | ✅ merged |
| #35 | Deep-research reports #6–8 (taxonomies, LLM cost, explainability) | docs | ✅ merged |
| #36 | Research-doc housekeeping + `measure_resolution.py` | chore | ✅ merged |
| #37 | Explainability — counterfactual recourse + honest precision | feat | ✅ merged |
| #38 | Human oversight (override) + bias-audit disclosure | feat | ✅ merged |
| #43 | `ARCHITECTURE.md` — system study guide + Mermaid diagrams | docs | ✅ merged |
| #44 | Reorder recruiter drill-in — verdict above answers | feat(ui) | ✅ merged |
| #45 | Demo target ad domain → data-analyst (bridge_fit N/A fix) | fix | ✅ merged |
| #46 | Czech presentation README (primary) + `docs/README.en.md` | docs | ✅ merged |
| #47 | Czech `ARCHITECTURE.cs.md` (1:1 translation) | docs | ✅ merged |
| #48 | `bridge_fit` as a months-to-ready estimate | feat | ✅ merged |
| #49 | `work` evidence tier — real employment no longer "weak" | fix | ✅ merged |
| #50 | Idempotent `_migrate_columns` — Postgres boot crash | fix | ✅ merged |
| #51 | Audit threshold slider (discriminating selection cutoff) | feat | ✅ merged |

**Not yet merged — flagged separately below:** #39–#42, a stacked branch
(`perf/llm-cost-haiku` → `perf/llm-structured-cache` → `perf/llm-prompt-caching`
→ `perf/haiku-ab-harness`), all opened 2026-06-02, all still `open`.

---

## Alignment with core assignment

Core assignment: AI matching platform that processes CVs (extraction of
personal data, education, work experience, skills, languages) and matches
candidates to job postings — focused on students / fresh-graduates /
career-changers compared fairly against experienced hires.

### Group 1 — Core pipeline correctness (ON TARGET, no notes needed)
`#26` (Postgres seq bug blocking all uploads), `#27` (Postgres upsert bug
blocking interest flow), `#29` (bootstrap wiping the prepared demo ad),
`#30`/`#31`/`#33` (candidate-type classifier: ad-relative career-changer
detection, brigáda-inflated tenure, real-vs-claimed work gate), `#45`
(bridge_fit domain mismatch), `#49` (real employment mis-tagged as weak
evidence), `#50` (Postgres boot crash). These are bug fixes and correctness
tightening directly inside the extract→classify→score pipeline — exactly the
core assignment's engine. All good, all necessary.

### Group 2 — Infra/perf (ON TARGET)
`#21` (truncate-guard safety), `#24` (role-scoped seed for cold-start speed),
`#23`/`#34` (seed rebakes to keep the demo DB current). Necessary
housekeeping for a deployed demo, not scope drift.

### Group 3 — UX polish (ON TARGET)
`#25` (form-batched recompute), `#44` (drill-in reorder), `#48` (bridge_fit
as months-to-ready instead of an abstract index), `#26`'s position-detail
panel for candidates. Minor, correctly scoped, improves the artifact
recruiters and candidates actually judge.

### Group 4 — Documentation (ON TARGET, but a heavy cadence)
`#32`, `#35`, `#36`, `#43`, `#46`, `#47` — 6 of 27 merged PRs (22%) are
docs-only. README rewrite, deep-research reports, ARCHITECTURE.md +
Mermaid, and **two full Czech translations** (README, ARCHITECTURE). None of
this is wrong — `#46` in particular (README as a live Czech presentation
script) is a deliberate, well-argued choice for a project heading into a
demo/interview context — but it's worth naming as a real cost center: a
quarter of the last review cycle's PR volume went to docs, not the pipeline
or matching quality.

### Group 5 — New direction: responsible-AI / governance layer (WORTH FLAGGING)
`#28` (evidence-strength + `MODEL_CARD.md` + demographic-blind extraction),
`#37` (counterfactual recourse, GDPR Art. 22 / CJEU C-203/22), `#38` (human
oversight override + bias-audit disclosure, EU AI Act Art. 14 / NYC LL144
direction), `#51` (audit threshold slider).

This is **not scope creep in the bad sense** — it's a coherent, well-cited
response to the platform's own `MODEL_CARD.md` classifying itself as
EU-AI-Act "high-risk." But it is a genuine **expansion beyond the literal
core assignment** ("extraction + matching") into compliance/audit tooling:
a new `analytics/audit.py` module, three new `Match` columns
(`recruiter_override`, `override_note`, `decision_at`), a bias-audit UI
panel with a four-fifths-rule computation and CSV export, and a
counterfactual-recourse UI surface for candidates. Four of the last 27 PRs
(15%) went here. Flagging so the owner can consciously decide whether this
is "MVP-necessary" (defensible — it's literally what makes a hiring-AI demo
credible to a technical or compliance-minded audience) or "gold-plating a
demo that doesn't need it yet" (also a defensible view, since the demo has
one target ad and 5–6 candidates — the four-fifths ratio on n≈5 isn't
statistically meaningful).

### No unrelated-domain drift
No auth system, no unrelated external integrations, no new business domain.
Everything merged since #21 is either (a) inside the CV→match pipeline, (b)
infra to keep the deployed demo alive, (c) docs, or (d) the compliance layer
discussed above, which is scoped tightly to *this* hiring-AI's own fairness
obligations, not a tangent.

---

## Flags / concerns

### F1 — All 27 PRs since #21 were authored/merged by `kuram39`, not `buhlez31` — resolved: GitHub username change

**Status: not a process gap — CLAUDE.md's repo reference is stale**

Every one of #21–#51 was opened from a fork and both opened and merged by
GitHub user `kuram39` (admin collaborator), not `buhlez31`. Pushing this
review's own branch confirms why: GitHub returned *"This repository moved.
Please use the new location: github.com/kuram39/cv-bau-students.git"*, and
the authenticated identity resolves to `kuram39` / **Martin Kura, Prague**
(`get_me`) — consistent with this being a Czech-market project built by its
Czech-based owner. This reads as a straightforward **GitHub username
rename** (`buhlez31` → `kuram39`), which GitHub auto-redirects, not a
second party or a bypassed review gate. The "owner merges PRs themselves"
convention in CLAUDE.md still holds; only the username on record is stale.

Recommendation: update CLAUDE.md's `GitHub:` line (currently
`buhlez31/cv-bau-students`) to the current `kuram39/cv-bau-students`, and
this session's own repo-scope config, so future sessions don't have to
re-derive this.

### F2 — Stale open stack: #39–#42 (`perf/llm-cost-haiku` et al.)

**Status: real, needed work — sitting idle for ~1 month**

This stack is the *exact* item CLAUDE.md lists as deferred ("LLM-cost
optimisation (8→4 calls per applicant) ... planned for a `perf/llm-cost`
branch"). #39 (Haiku model-tiering, flag-gated), #40 (A/B harness), #41
(structured outputs), #42 (prompt caching) are all well-scoped, ship dark
(flags default off), and explicitly say they need the owner's live API key
to validate before flipping on — consistent with CLAUDE.md's "agent cannot
make live LLM calls" constraint. They were opened 2026-06-02 and have not
moved since. Recommendation: this is a single owner session away (run the
A/B harness with a real key, confirm Czech-extraction quality holds on
Haiku, flip flags) from closing out the biggest deferred cost lever in
`docs/RISKS.md`. Either schedule that session or close the stack if it's no
longer a priority — four open PRs with no forward motion is worse than
neither state.

### F3 — Compliance surface may be ahead of what the demo can substantiate

**Status: low risk, but noted**

The bias-audit (`#38`, `#51`) computes an EEOC four-fifths ratio over
whatever candidates exist for the one seeded target ad (5–6 demo personas).
Statistically this is closer to a UI mockup of what an audit *would* look
like at scale than a real audit. `MODEL_CARD.md` already documents the "no
'unbiased' claim" stance per #28, which is the right caveat — just flagging
that the audit UI's numbers could be over-read by a viewer who doesn't
notice the caption. No code change needed; worth double-checking the
audit panel's copy makes n≈5 obvious at a glance.

### F4 — One-time production data fixes may not have been run on Neon

**Status: verify, not code**

Three PRs shipped code fixes paired with a manual one-time SQL step the
*owner* needs to run against the live Neon DB: #45 (`UPDATE job_ads SET
domain='data-analyst' ... ; rescore_ad(341)`), #49
(`scripts/retag_work_evidence.py --rescore-ad 341`), and #26's out-of-band
sequence reset (already stated as done). If any of these three were missed,
the live deploy would silently show stale bridge_fit/evidence values even
though `main` is correct. Worth a 2-minute check against the Neon dashboard
next time the owner is in there.

---

## Open deferred items (carried forward)

| Item | Priority | Notes |
|---|---|---|
| Merge #39–#42 (LLM-cost, Haiku tiering) | High | See F2. Biggest lever on demo latency/cost; blocked only on an owner session with a live key. |
| Update CLAUDE.md `GitHub:` line to `kuram39/cv-bau-students` | Low | See F1 — cosmetic, repo renamed (`buhlez31`→`kuram39`), GitHub redirects fine either way. |
| Vendor tool aliases (Power BI, Tableau, Excel → ESCO) | Medium | Carried from 2026-06-01 review; research #6 (in #35) confirms Czech aliasing as the main resolution gap — `measure_resolution.py` (#36) is now available to quantify it before further work. |
| Verify Neon one-time SQL steps ran (#45, #49) | Medium | See F4. |
| `personal_fit` retirement cleanup | Low | Carried from 2026-06-01 review; still not done — schema field + `personal_fit=0.0` line. |

---

## Current state summary

**Concept: intact.** The core pipeline (extract → translate capabilities →
resolve into ESCO → score coverage against a recruiter-curated target set →
surface to a recruiter with evidence tiers and recourse) is unchanged in
shape since the 2026-06-01 review. The 27 PRs in this window are
overwhelmingly correctness fixes (classifier, evidence tiering, Postgres
bugs) and demo-readiness work (seed rebakes, Czech docs), not architecture
change.

**New direction since last review: a responsible-AI/governance layer**
(human-override recording, bias-audit panel, counterfactual recourse,
`MODEL_CARD.md`). This is the one real addition beyond "extract + match" —
well-justified by the platform's own high-risk self-classification, but
worth the owner consciously deciding how much further to invest here versus
the still-deferred LLM-cost work (F2), given both compete for the same
scarce "owner session with a live key" time.

**No process risk found:** the owner-merges-themselves convention held
throughout — F1 turned out to be a stale GitHub username reference
(`buhlez31` renamed to `kuram39`), not a bypassed review gate.

**Recommendation, ranked:**
1. Spend the next owner+key session on #39–#42 (Haiku tiering) — it directly
   retires the largest item in `docs/RISKS.md` and is fully prepared, just
   unvalidated.
2. Update CLAUDE.md's `GitHub:` line to the current repo name (F1) — cheap,
   avoids re-deriving the "did the repo move?" question next review.
3. Hold the compliance-layer scope where it is (#28/#37/#38/#51 already
   ship a credible story) rather than adding more audit surface until the
   demo has more than ~6 candidates to make the numbers meaningful (F3).
4. Quick Neon check for the #45/#49 one-time SQL steps (F4) — cheap
   insurance against a silently stale prod demo.

https://claude.ai/code/session_01NTmUxrK5RXg1c6UofiFmW8
