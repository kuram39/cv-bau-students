# PR Review — 2026-06-28

Automated periodic review of merged pull requests and project state.
Previous tracking point: PRs #1–#3 (noted in CLAUDE.md). This review
covers PRs #28–#51 (latest batch merged 2026-06-01 / 2026-06-02).

---

## Merged PRs since last check

| # | Type | Title | Assessment |
|---|---|---|---|
| #28 | feat | Evidence-strength + transparency + bias hardening | Core ✅ |
| #29 | fix | Bootstrap: skip scraped-ad overlay when already seeded | Core ✅ |
| #30 | feat | Classify candidate type relative to the target ad | Core ✅ |
| #31 | fix | No/low real experience → student, not experienced | Core ✅ |
| #32 | docs | README rewrite to current state | Docs ✅ |
| #33 | fix | Codex P2 edge-cases: real_work_years, strongest tier wins, fallback evidence | Core ✅ |
| #34 | chore | Rebake seed.sqlite.gz | Maintenance ✅ |
| #35 | docs | Deep-research reports #6-8 (vendor taxonomies, LLM cost, explainability) | Docs ✅ |
| #36 | chore | Research housekeeping + measure_resolution script | Maintenance ✅ |
| #37 | feat | Explainability: counterfactual recourse + honest precision (GDPR-aligned) | Core ✅ |
| #38 | feat | Human oversight (override) + bias-audit disclosure (EU AI Act Art. 14) | Core ✅ |
| #43 | docs | ARCHITECTURE.md with Mermaid diagrams | Docs ✅ |
| #44 | feat | Recruiter drill-in: verdict above candidate answers | Core ✅ |
| #45 | fix | Target ad domain → data-analyst (bridge_fit was N/A) | Core ✅ |
| #46 | docs | Czech presentation README (primary README rewritten) | Docs ✅ |
| #47 | docs | Czech ARCHITECTURE.cs.md (full translation) | Docs ✅ |
| #48 | feat | bridge_fit → months-to-ready estimate | Core ✅ |
| #49 | fix | 'work' evidence tier: real employment no longer weak | Core ✅ |
| #50 | fix | Idempotent _migrate_columns — Postgres boot crash (DuplicateColumn) | Core ✅ |
| #51 | feat | Audit threshold slider — discriminating selection cutoff | Core ✅ |

**PR number gap: #39–#42** — four PRs in this range do not appear as merged.
They were likely opened and closed without merge (draft / abandoned). No action needed,
but worth confirming manually if there were in-progress experiments in that range.

**Direct-to-main commit (policy violation):**
Commit `0c920a8` ("docs: spinner ~2 LLM volání") was pushed directly to `main`,
bypassing the Branch → PR → merge requirement from CONTRIBUTING.md.
The change itself is trivial (spinner text "~4" → "~2 LLM volání") but it sets
a precedent. Future micro-fixes should still go through a PR — CI only runs on PRs.

---

## Alignment with core assignment

**Core assignment:** AI matching platform that processes CVs (personal data, education,
work experience, skills, languages) and matches candidates to job postings.

Every merged PR directly advances this or supports it. No scope creep detected.
Specific alignments:

- **CV extraction** — `real_work_years`, evidence tiers, brigáda classification (#33, #31, #49)
  sharpen the accuracy of what gets read from a CV and how it's weighted.
- **Matching** — counterfactual recourse (#37), bridge-months (#48), audit slider (#51)
  add recruiter-facing depth on top of the existing skill_fit / bridge_fit axes.
- **Pipeline correctness** — ad-relative classifier (#30), target ad domain fix (#45),
  idempotent migration (#50) keep the two-pass journey working in production.
- **Compliance** — human override (#38), Model Card (#28), demographic blind prompts (#28)
  position the system correctly for EU AI Act high-risk classification.

---

## Concept health

**Is the concept intact?** Yes — the core idea (% coverage of target skill set replaces
"years of experience" as the comparison axis, with doloženost expressing evidence quality)
has not changed. All 20 PRs either refine this idea or make it work more reliably.

**New directions since last review (not scope creep — expected evolution):**

1. **Single-target MVP is now explicit.** The README and architecture docs make clear
   this scores one recruiter-curated target ad, not an entire corpus. The corpus-ranking
   path (`rank_candidate` / `find_candidate_ads`) exists in code but is not the demo flow.
   The documentation is now honest about this — good.

2. **EU AI Act / GDPR alignment is first-class.** The project now ships a Model Card,
   counterfactual recourse, human-override UI, and bias-audit log. These are the right
   building blocks for a high-risk AI system under Annex III(4).

3. **Czech-first presentation.** The primary README is now in Czech. This is intentional
   (the target audience is Czech recruiters / a Czech interview panel). The English version
   is preserved at `docs/README.en.md`.

---

## Risks and flags

### 🔴 High — must address before demo / production

None introduced by recent PRs. PR #50 actually *fixed* the highest-risk production issue
(Postgres boot crash).

### 🟡 Medium — deferred but documented

1. **Skill resolution at 21% (Layer-1 only).** `scripts/measure_resolution.py` now
   exists (PR #36) and RISKS.md reports Layer-1 = 21% on the 6 demo CVs. The LLM
   `esco_term` bridges most of the gap, but the *combined* resolution rate is not yet
   measured. **Recommendation:** run `measure_resolution.py` on a reseeded demo DB and
   record the combined (lexical + LLM) rate in RISKS.md before any interview or pitch.
   If combined resolution is below ~70%, ESCO Czech alias loading becomes urgent.

2. **perf/llm-cost branch is ready to ship.** RISKS.md documents the 8→4 call reduction
   precisely. Research #7 confirmed the path. The implementation is well-understood
   (defer `reason_for_ranking` to express_interest; reuse translate result). If cost is
   a demo talking point, shipping this branch would strengthen the answer.

3. **Corpus prefilter ignores curated target skills.** RISKS.md §"Known gap" — the
   SQL pre-filter doesn't consult `ad_target_skills` or ESCO capability `skill_id`s.
   Fine for the single-target demo, matters the moment multi-ad corpus ranking becomes
   a product feature.

4. **No Neon monitoring / alerting.** RISKS.md Tier 3, #14: no structured logs,
   no Sentry/Datadog. The Postgres crash in PR #50 was found manually.

### 🟢 Low / acknowledged

- Streamlit single-process concurrency (intentional for demo).
- Bridge months calibration (8 hand-curated domains; defensible).
- Czech ISCO crosswalk (NSP aliases > full CZ-ISCO map — correct decision).

---

## Recommendations

### 1. Measure combined skill resolution now

```bash
# From a reseeded demo DB (after seed_target_demo.py)
./venv/bin/python -m scripts.measure_resolution
```

Record the combined % in RISKS.md § "Phase C". If <70%, escalate ESCO Czech alias
loading. This unblocks the interview answer on "jak přesně rozumíte českým CV?"

### 2. Ship perf/llm-cost (8 → 4 calls)

The branch name is documented; the work is defined in RISKS.md §"LLM cost".
Two changes (defer reasoning, reuse translate) are mechanical and do not touch
the scoring logic. Tests should stay green with no changes. Cost halving is a
strong talking point against "isn't LLM-backed matching expensive?"

### 3. Enforce Branch → PR for all changes, including one-liners

The direct-to-main commit `0c920a8` is a canary: once it's acceptable for small
changes, the convention erodes. CI does not run on direct pushes to `main`.
If the hook `docs:` prefix suggests it's documentation, note that spinner text
lives in `candidate_panel.py` (Python source) and belongs in a PR.

### 4. Close the #39–#42 gap (investigation only)

Verify manually that PRs #39–#42 are not half-implemented branches that were
abandoned mid-flight. If they are draft PRs with unfinished code on open branches,
clean up those branches to avoid confusion.

### 5. Add combined resolution metric to RISKS.md before next interview

The current entry says "Layer-1 21%" but leaves LLM-assisted resolution unmeasured.
A blank there reads as uncertainty. Fill it with the real number.

---

## Summary

Concept: fully intact. The platform correctly translates CV evidence into skill-coverage
scores, surfaces evidence quality (doloženost), provides counterfactual recourse for
candidates and recruiters, and now has production-hardened Postgres persistence with
EU AI Act-aligned human oversight and bias audit tooling.

Pace: 20 PRs in two days (2026-06-01/02) — clearly a burst of pre-demo polish, not
ongoing daily velocity. The backlog is small and well-documented.

Next priority: run measure_resolution.py → decide on Czech alias loading; ship
perf/llm-cost branch.
