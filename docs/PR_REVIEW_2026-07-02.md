# PR Review — 2026-07-02

Auto-generated alignment check. Covers **PRs #21–#51** (merged since last
review PR #22, which covered #7–#20 and merged at #22 itself). PRs #39–#42
(Haiku tiering / structured outputs / prompt caching / A-B harness) are
**open, not merged** — flag-gated, ships-dark, waiting on the owner's key to
validate — noted below but not counted as "merged since last check."

---

## Merged PRs since last check (27)

| # | Title | Type | Status |
|---|---|---|---|
| #21 | Guard `--truncate` against wiping candidate data (R6) | fix | ✅ merged |
| #22 | PR review (2026-06-01) + stale-annotation cleanup + thinking default ON (F1) | docs+fix | ✅ merged |
| #23 | Rebake `seed.sqlite.gz` with post-MVP demo | chore | ✅ merged |
| #24 | Role-scoped seed — shrink DB to data-analyst demo (~200k→~2k rows) | perf | ✅ merged |
| #25 | Recompute target skills only on Save (`st.form`) | fix(ui) | ✅ merged |
| #26 | Postgres seq reset (uploads) + show position detail to candidate | fix+feat | ✅ merged |
| #27 | `record_interest` dialect-agnostic upsert (Postgres compat) | fix | ✅ merged |
| #28 | Evidence-strength + transparency + bias hardening (deep-research A+B+C) | feat | ✅ merged |
| #29 | Skip scraped-ad overlay when `job_ads` already seeded | fix(bootstrap) | ✅ merged |
| #30 | Classify candidate type relative to the target ad | feat(detector) | ✅ merged |
| #31 | No/low real experience → student, not experienced | fix(detector) | ✅ merged |
| #32 | Rewrite README to current state | docs | ✅ merged |
| #33 | Codex P2 edge-cases in evidence + classifier | fix | ✅ merged |
| #34 | Rebake `seed.sqlite.gz` from fresh reseed | chore | ✅ merged |
| #35 | Deep-research reports #6–8 (vendor taxonomies, LLM cost, explainability) | docs | ✅ merged |
| #36 | Research-doc housekeeping + `measure_resolution.py` | chore | ✅ merged |
| #37 | Explainability — counterfactual recourse + honest precision | feat | ✅ merged |
| #38 | Human oversight (override) + bias-audit disclosure | feat | ✅ merged |
| #43 | `docs/ARCHITECTURE.md` — system study guide + Mermaid diagrams | docs | ✅ merged |
| #44 | Reorder recruiter drill-in — verdict above answers | feat(ui) | ✅ merged |
| #45 | Demo target ad domain → data-analyst (fixes `bridge_fit` N/A) | fix | ✅ merged |
| #46 | Czech presentation README (primary) + `docs/README.en.md` | docs | ✅ merged |
| #47 | `docs/ARCHITECTURE.cs.md` (1:1 translation) | docs | ✅ merged |
| #48 | `bridge_fit` as a months-to-ready estimate | feat | ✅ merged |
| #49 | `work` evidence tier — real employment no longer weak | fix | ✅ merged |
| #50 | Idempotent `_migrate_columns` — Postgres boot crash (DuplicateColumn) | fix | ✅ merged |
| #51 | Audit threshold slider | feat | ✅ merged |

**Open, not merged:** #39 (Haiku model-tiering), #40 (Haiku A/B harness),
#41 (structured-output `call_json`), #42 (prompt caching) — a stacked
branch chain implementing deep-research finding #7's ranked LLM-cost levers,
all flag-gated off by default. This is the top item from the last two
reviews' deferred list; good that it's in flight. Needs the owner's key to
A/B-validate Czech extraction quality on Haiku before enabling.

---

## Alignment with core assignment

Core assignment: AI matching platform that processes CVs (extraction of
personal data, education, work experience, skills, languages) and matches
candidates to job postings — with specific focus on students /
fresh-graduates / career-changers compared fairly against experienced hires.

### Directly on target (21 of 27)

#21, #23, #24, #25, #26 (bug half), #27, #29, #30, #31, #32, #33, #34, #36,
#43, #44, #45, #46, #47, #48, #49, #50 — bug fixes, perf, docs, and UX
polish squarely inside the CV-extraction → classify → score → present
pipeline. Two call-outs:

- **#30/#31/#33** — the candidate-type classifier (student / career_changer
  / experienced) moved from a weak self-reported-`target_domains` signal to
  a real signal (work history vs. the target ad's domain, real vs. brigáda
  years). This is a genuine correctness fix at the center of the product's
  differentiator (fair comparison across candidate types) — the right kind
  of investment.
- **#45/#48/#49** — bridge_fit and doloženost (evidence tier) bug fixes are
  exactly the kind of "is the score honest" work the concept depends on
  (RISKS.md Tier-4 already flagged bridge_fit lying at 100 pre-fix; #49 was
  a real regression where genuine work experience displayed as "unproven").

### Scope expansion — compliance/audit tooling (#28, #37, #38, #51)

Four PRs (of 27) build a **bias-audit and human-oversight layer** on top of
the matcher: `MODEL_CARD.md`, demographic-blind extraction prompts,
counterfactual recourse ("doložit X → +N% coverage"), a recruiter
"human oversight" override + note field (3 new DB columns), and a
four-fifths-rule selection-rate audit dashboard with a threshold slider.

This is **not** part of the literal core assignment ("processes CVs and
matches candidates to job postings"). It's defensible — CV-screening AI is
squarely in EU AI Act Annex III / GDPR Art. 22 territory, so a bias-audit +
override story is a legitimate hedge for an interview-round project, and it
was research-backed (deep-research #8, 3/3 adversarial-verified) rather
than speculative. But it **is** a new product surface (its own UI expander,
its own schema columns, its own CSV export) built during a window when the
two items this review and the last one both flagged as highest-priority —
LLM-cost optimization and Czech skill-resolution/vendor-tool aliases — were
still sitting deferred. Worth an explicit owner call: is audit/oversight
tooling part of what's being graded, or overhead that competed with core
pipeline work for the same PR budget?

### Continued MVP narrowing (#24, #45)

Consistent with the single-target MVP direction flagged (not as drift) in
the last review. `build_scoped_seed.py` now ships a ~2k-row DB scoped to the
data-analyst occupation family, and the demo ad's domain is hardcoded to
`data-analyst` so `bridge_fit` has a rubric. The general "match against any
job ad in a corpus" path still exists in code (`rank_candidate` /
`find_candidate_ads`) but is bypassed by the demo's direct single-ad score
path — and RISKS.md's own "Known gap" section already documents that the
corpus prefilter doesn't consult curated target skills, so that path isn't
production-ready today even if re-enabled. Not new risk, just confirming the
gap between "what the demo shows" and "what the schema/code generally
supports" hasn't closed.

### No hard drift

No PRs add anything outside the hiring-matching domain (no unrelated
verticals, no auth/payments/analytics-for-analytics'-sake). The
audit/oversight work, while scope expansion relative to the literal
assignment text, stays inside the same domain (fairness of a hiring-AI
decision) rather than wandering into an unrelated feature area.

---

## Flags / concerns

### G1 — `CLAUDE.md` itself is now stale (concrete, actionable)

`CLAUDE.md`'s Architecture section still describes "3-axis scoring
(skill_fit / bridge_fit / personal_fit)" and a "16-table schema" — both
wrong since #20 (3-axis retired) and #38 (18 tables + 3 new Match columns).
The "Current state / open items" list predates all 27 PRs in this review:
no mention of the evidence-tier/doloženost system, the ad-relative
classifier, human-oversight override, bias audit, counterfactual recourse,
`docs/ARCHITECTURE.md`/`MODEL_CARD.md`, the Czech-primary README, the
Postgres/Neon deploy path, or the open LLM-cost PR stack (#39–#42). Since
this file is the first thing loaded into every future session's context
(including this one), staleness here compounds — recommend a follow-up
`docs/` PR to resync it against `docs/ARCHITECTURE.md` (already accurate,
per #43/#47).

### G2 — Two "prod down" Postgres bugs in one review window

#26 (id-sequence desync blocked **all** uploads) and #50 (boot-crash
DuplicateColumn on every restart) were both post-hoc fixes for the same
subsystem: the SQLite→Postgres migration/auto-migrate path. Both slipped
past CI because the test suite runs against SQLite only (dialect-agnostic
code, not dialect-tested). Two live-breaking bugs in the same subsystem
within one review window is a signal, not a coincidence — worth a
Postgres-flavored smoke test (even a local `docker run postgres` in CI, or
a nightly job against the real Neon branch) before the next schema-touching
PR, rather than continuing to discover these in prod.

### G3 — Repo owner mismatch

`CLAUDE.md` states `GitHub: buhlez31/cv-bau-students`; the GitHub API
resolves this session's calls to `kuram39/cv-bau-students` (same repo,
presumably a GitHub username change/alias — content and PR history line up
1:1). Low risk, but worth a one-line confirmation + `CLAUDE.md` fix so the
canonical repo path doesn't silently drift.

### Carried forward, now resolved

- **F1** (`LLM_THINK_ENABLED` defaulting OFF) — ✅ fixed in #22, default is
  now ON (`config.py:42`).
- **F2/F3** (dead `WEIGHT_*` constants, stale `score.py` docstring) — ✅
  annotated/fixed in #22.
- **F4** (personal_fit abandoned) — unchanged, still low-risk/schema-only.
- **F5** (owner-verify note on #20) — resolved by elapsed time and the
  subsequent demo-hardening PRs (#23, #24, #34).

---

## Open deferred items (carried / updated)

| Item | Priority | Notes |
|---|---|---|
| LLM-cost optimization | High | **In flight**, not merged: #39–#42 stack (Haiku tiering, structured outputs, prompt caching, A/B harness), flag-gated, needs owner key to validate Czech-extraction quality before enabling. |
| Vendor tool aliases (Power BI, Tableau, Excel → ESCO) | Medium | Still ~30% of student skill-phrase misses per `measure_resolution.py` (#36); untouched this window — deprioritized behind the audit/oversight work (see G-scope-expansion above). |
| `CLAUDE.md` resync | Medium (new) | See G1. Concrete drift between the project's own onboarding doc and 27 merged PRs of actual state. |
| Postgres dialect test coverage | Medium (new) | See G2. Two prod-down bugs in one window from the same untested-dialect gap. |
| `personal_fit` retirement cleanup | Low | Delete the schema field + `personal_fit=0.0` line after a DB migration cycle. Unchanged. |
| Corpus-wide prefilter gap (curated target skills) | Low | Documented in RISKS.md "Known gap"; doesn't bite the single-target demo. Unchanged. |

---

## Current state summary

**Concept: intact.** Every merged PR stays inside the CV-extraction →
classify → coverage-score → explain pipeline for the hiring domain. The
classifier correctness fixes (#30/#31/#33) and evidence-tier fixes
(#45/#48/#49) are real quality wins at the center of the product's stated
value (fair comparison of students/career-changers against experienced
hires).

**New direction this window: compliance/audit as a first-class feature.**
#28/#37/#38/#51 built out a bias-audit dashboard, human-override workflow,
and counterfactual recourse — a genuine new product surface, not just
polish. It's well-motivated (EU AI Act/GDPR framing already existed in
RISKS.md/MODEL_CARD.md) and stays in-domain, so this review does not call
it drift. It does represent a deliberate reallocation of PR effort away
from the two items both this review and the last flagged as highest
priority (LLM cost, Czech resolution) — worth an explicit owner decision on
whether that trade was intentional.

**Main risks:** (1) `CLAUDE.md` itself has drifted from reality across 27
PRs (G1) — the fix is cheap and should happen soon since it's the context
every future session (human or agent) starts from; (2) the Postgres
migration path has now broken production twice from the same untested-gap
class (G2).

**Recommendation for what to prioritize next:** land the #39–#42 LLM-cost
stack (owner validates with key — biggest lever, already built, just
gated), then a small `docs/` PR to resync `CLAUDE.md`, then return to the
vendor-tool-alias gap before adding further audit/compliance surface area.

https://claude.ai/code/session_01LyH8BCjE4jzt7L413TjEpe
