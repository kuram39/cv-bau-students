# PR Review — 2026-07-05

Auto-generated alignment check. Covers **PRs #21, #23–#38, #43–#51** (merged
since last review PR #22, which covered #7–#20) plus 2 direct-to-`main`
commits with no PR. No new activity found between 2026-06-02 and today
(2026-07-05) — the batch below is everything merged on 2026-06-01/02, and the
trail goes cold after that.

---

## Merged PRs since last check

| # | Title | Type | Status |
|---|---|---|---|
| — | Added Dev Container Folder | chore | ⚠️ direct commit to `main`, no PR |
| #21 | Guard `--truncate` against wiping candidate data (R6) | fix | ✅ merged |
| #23 | Rebake seed.sqlite.gz with post-MVP demo | chore | ✅ merged |
| #24 | Role-scoped seed — shrink DB to data-analyst demo (~200k→~2k rows) | perf | ✅ merged |
| #25 | Recompute target skills only on Save (st.form) + info tooltip | fix(ui) | ✅ merged |
| #26 | Postgres id-sequence reset after migrate + candidate position detail | fix | ✅ merged |
| #27 | `record_interest` dialect-agnostic upsert (Postgres compat) | fix | ✅ merged |
| #28 | Evidence-strength + transparency + bias hardening (research A+B+C) | feat | ✅ merged |
| #29 | Skip scraped-ad overlay when job_ads already seeded | fix | ✅ merged |
| #30 | Classify candidate type relative to the target ad | feat(detector) | ✅ merged |
| #31 | No/low real experience → student, not experienced | fix(detector) | ✅ merged |
| #32 | Rewrite README to current state | docs | ✅ merged |
| #33 | Address Codex P2 edge-cases in evidence + classifier | fix | ✅ merged |
| #34 | Rebake seed.sqlite.gz from fresh reseed | chore | ✅ merged |
| #35 | Deep-research reports #6–8 (vendor taxonomies, LLM cost, explainability) | docs | ✅ merged |
| #36 | Research-doc housekeeping + measure_resolution script | chore | ✅ merged |
| #37 | Explainability — counterfactual recourse + honest precision | feat | ✅ merged |
| #38 | Human oversight (override) + bias-audit disclosure | feat | ✅ merged |
| #43 | ARCHITECTURE.md — system study guide with Mermaid diagrams | docs | ✅ merged |
| #44 | Reorder recruiter drill-in — verdict above answers | feat(ui) | ✅ merged |
| #45 | Demo target ad domain → data-analyst so bridge_fit computes | fix | ✅ merged |
| #46 | Czech presentation README (primary) + English → docs/README.en.md | docs | ✅ merged |
| #47 | Czech translation of ARCHITECTURE (ARCHITECTURE.cs.md) | docs | ✅ merged |
| #48 | Present bridge_fit as months-to-ready estimate | feat | ✅ merged |
| #49 | Add 'work' evidence tier so real employment isn't tagged weak | fix | ✅ merged |
| #50 | Make `_migrate_columns` idempotent — Postgres boot crash fix | fix | ✅ merged |
| #51 | Audit threshold slider — find a discriminating selection cutoff | feat | ✅ merged |
| — | "docs: spinner ~2 LLM volání" (UI copy, `--think`→2-call count fix) | fix(ui) | ⚠️ direct commit to `main`, no PR |

**#39–#42 are open, unmerged, NOT part of this review** (see Open Items below).

CI is green (ruff + black + pytest) on every commit through current `main`
HEAD (`0c920a8`).

---

## Alignment with core assignment

Core assignment: AI matching platform that processes CVs (extraction of
personal data, education, work experience, skills, languages) and matches
candidates to job postings — with specific focus on students /
fresh-graduates / career-changers compared fairly against experienced hires.

### All merged PRs: ON TARGET

- **#21, #26, #27, #29, #50** — Postgres/migration correctness fixes
  (truncate guard, id-sequence reset, dialect-agnostic upsert, idempotent
  column migration). Infrastructure hardening for the deployed demo, not drift.
- **#23, #24, #34** — Seed data shrink + rebakes. #24 is a good instinct:
  scoping the taxonomy to the data-role family cut cold-start from ~200k rows
  to ~2k with a verified no-op on scoring. Correctly reasoned, tested.
- **#25, #44** — UI ordering/behavior polish. Minor, correctly scoped.
- **#28, #30, #31, #33, #37, #38, #49, #51** — this is the meat of the batch:
  a coherent, well-sequenced push to make the fairness/evidence/oversight
  story real, all traced back to the `docs/research/01–08` deep-research
  reports (EU AI Act, calibration, bias, self-report verification,
  explainability):
  - Evidence tiers (🟢work/🟡project/⚪claimed) replace a meaningless
    LLM-confidence number — directly improves the honesty of what the
    recruiter sees for a student vs. an experienced hire.
  - Candidate-type classification relative to the actual target ad (not a
    self-stated field that was usually empty) — fixes a real bug in the
    core "is this a student/career-changer/experienced" comparison the
    whole product is about.
  - Counterfactual recourse ("doložit X → +N%"), human-oversight override,
    bias-audit disclosure with four-fifths ratio — these are exactly the
    features a hiring-adjacent AI tool needs for EU AI Act Art. 14/86 and
    GDPR Art. 22, and they're implemented as deterministic, zero-LLM,
    offline re-scores. Disciplined, in-scope, well-tested (each PR includes
    a Codex-P2-edge-case fix landing within hours of the feature).
  - #49 fixed a genuine correctness bug (real employment defaulting to the
    weakest evidence tier) that was actively mis-representing experienced
    candidates — a core-pipeline bug, not a tangent.
- **#32, #35, #36, #43, #46, #47** — docs (README rewrite, ARCHITECTURE.md +
  Czech translation, deep-research writeups). Volume is high but all
  documentation, zero product-code risk.
- **#45, #48** — demo-quality fixes (ad domain so bridge_fit isn't N/A;
  bridge_fit reframed as a months-to-ready estimate). Correctly scoped
  polish of an existing metric, not a new axis.

### No scope-creep detected in the merged set

Nothing here adds a feature outside the CV→match pipeline (no auth system,
no unrelated integrations, no new business domain). The fairness/audit
additions (#28, #37, #38, #51) expand the product's *surface* meaningfully,
but they're squarely inside "match candidates to jobs fairly" — arguably the
single most important axis for a tool explicitly framed (per CLAUDE.md) as
helping non-traditional candidates get compared fairly.

---

## Flags / concerns

### G1 — Repo access model changed from "solo" to "admin collaborator" — contradicts CLAUDE.md (NEW, notable)

**Status: governance drift, not code drift — flagging for owner confirmation**

CLAUDE.md states: *"GitHub: `buhlez31/cv-bau-students` (private, standalone —
NOT a GitHub fork)"* and *"The owner merges PRs themselves (solo repo)."*

`list_repository_collaborators` currently shows a second account, **`kuram39`,
with `admin` role** on the repo. That account has forked the repo
(`kuram39/cv-bau-students`) and opened a 4-PR stack (#39–#42, all still open)
implementing exactly the `perf/llm-cost` deferred item flagged in the last
two reviews (Haiku model-tiering, A/B harness, structured-output tool-use,
prompt caching — all flag-gated/"ships dark", needs the owner's live key to
validate). The work itself looks careful (flags default off, tests green,
explicit owner-verify steps), but **admin is a high-privilege grant** (can
push directly to `main`, edit the CI workflow, change branch protection,
manage other collaborators) and its addition isn't reflected anywhere in the
project docs. Recommend: (a) confirm this was an intentional add, (b) if
`kuram39` is meant only to propose PRs, downgrade to `write` or `triage`
role, (c) update CLAUDE.md's "solo repo" framing either way so it stays
accurate.

### G2 — Two direct-to-`main` commits bypass the branch → PR → merge convention

**Status: minor, self-inflicted process gap — worth a nudge, not a fire drill**

CLAUDE.md: *"Branch → PR → merge. Never commit to `main` directly."*
"Added Dev Container Folder" (`60d6306`) and "docs: spinner ~2 LLM volání"
(`0c920a8`) both landed as direct commits to `main`, authored by `buhlez31`
— no PR, no CI gate run before merge (CI *did* run post-hoc and passed, but
only because branch protection isn't hard-enforced, per CLAUDE.md's own
caveat). Both changes are low-risk (a devcontainer config; a one-line UI
string fix), so no harm done here — but it's the exact gap the "don't merge
a red PR" convention exists to catch, and it's now happened twice in one
session. Worth a reminder-to-self more than a process overhaul.

### G3 — Deferred `perf/llm-cost` item now has real work in flight, but stalled

**Status: informational — not actionable without the owner's key**

The single biggest deferred item from the last two reviews (8→4 LLM calls
per CV) is being addressed in PRs #39–#42, but all four have been open and
untouched since 2026-06-02 (over a month as of this review) — they need the
owner's live API key to A/B-validate Haiku quality and inspect cache-token
usage before any of them can merge. Not a code issue, just a reminder this
is the next thing waiting on the owner, not on more agent work.

### F1 (carried, resolved) — `LLM_THINK_ENABLED` default

The previous review flagged thinking silently defaulting off. Resolved in
PR #22 itself (per its own title: "...thinking default ON (F1)"). No longer
an open item.

### F2–F5 (carried) — dead-weight weights, stale docstrings, personal_fit cleanup

All still low-priority/cosmetic; no new information this cycle. `#20`'s
`personal_fit=0.0` line is still present; still a candidate for the deferred
cleanup, not urgent.

---

## Open deferred items (carried + updated)

| Item | Priority | Notes |
|---|---|---|
| `perf/llm-cost` (8→4 LLM calls/CV) | High | **In progress but stalled** — PRs #39–#42 open since 2026-06-02, blocked on owner's key for A/B validation (see G3). |
| Repo access model / CLAUDE.md accuracy | High (new) | `kuram39` added as `admin` collaborator; docs still describe a solo repo (see G1). |
| Vendor tool aliases (Power BI, Tableau, Excel → ESCO) | Medium | Unchanged since last review. |
| `personal_fit` retirement cleanup | Low | Still present as dead `0.0` field; unchanged. |
| README / Mermaid refresh for dual-panel flow | Done | Superseded by #32 (README rewrite) + #43 (ARCHITECTURE.md + Mermaid). Remove from tracking. |

---

## Current state summary

**Concept: intact, and now materially more defensible.** The core pipeline
(extract → resolve to ESCO → score against a curated target skill set →
present to a recruiter) is unchanged in shape. What changed this cycle is
depth, not direction: evidence-tiered scoring, ad-relative candidate typing,
counterfactual recourse, human-oversight override, and bias-audit disclosure
all sharpen the product's honesty and defensibility for its stated audience
(students/career-changers compared fairly against experienced hires) without
adding anything outside the CV→match domain. Every feature PR in this batch
traces to a deep-research doc and shipped with same-day Codex-P2 follow-up
fixes — a disciplined build-fix-verify rhythm.

**New risk this cycle is organizational, not architectural.** The product
code is clean; the repo's access model quietly diverged from what CLAUDE.md
documents (G1) and the branch-PR-merge convention was bypassed twice (G2).
Neither is a product bug, but both are worth the owner's five minutes:
confirm/adjust the `kuram39` admin grant, and re-affirm (or relax) the
direct-commit rule.

**Trail goes cold after 2026-06-02.** No merges in the ~month since. The
open PR stack (#39–#42) is the one piece of unfinished business actively
waiting on the owner rather than on further agent work.
