# PR Review — 2026-07-03

Auto-generated alignment check. Covers **PRs #21, #23–#38, #43–#51** (26 PRs
merged since the last review, PR #22, which shipped `PR_REVIEW_2026-06-01.md`
covering #7–#20). #39–#42 are open/unmerged (see "Open work" below) and are
therefore out of scope for this check.

All 26 PRs merged within a tight window, **2026-06-01 06:54 → 2026-06-02
14:14**. **No PR has merged in the ~30 days since** (last merge: #51,
2026-06-02T14:14:14Z; today: 2026-07-03).

---

## Merged PRs since last check

| # | Title | Type | Status |
|---|---|---|---|
| #21 | Guard `--truncate` against wiping candidate data (R6) | fix | ✅ merged |
| #23 | Rebake `seed.sqlite.gz` (post-MVP demo) | chore | ✅ merged |
| #24 | Role-scoped seed — shrink DB ~200k→~2k rows | perf | ✅ merged |
| #25 | Recompute target skills only on Save (`st.form`) | fix(ui) | ✅ merged |
| #26 | Postgres seq reset (uploads) + show position detail to candidate | fix/feat | ✅ merged |
| #27 | `record_interest` dialect-agnostic upsert (Postgres compat) | fix | ✅ merged |
| #28 | Evidence-strength + transparency + bias hardening (research A+B+C) | feat | ✅ merged |
| #29 | Skip scraped-ad overlay when `job_ads` already seeded | fix | ✅ merged |
| #30 | Classify candidate type relative to the target ad | feat | ✅ merged |
| #31 | No/low real experience → student, not experienced | fix | ✅ merged |
| #32 | Rewrite README to current state | docs | ✅ merged |
| #33 | Codex P2 edge-cases in evidence + classifier | fix | ✅ merged |
| #34 | Rebake `seed.sqlite.gz` from fresh reseed | chore | ✅ merged |
| #35 | Deep-research reports #6–8 (taxonomies, LLM cost, explainability) | docs | ✅ merged |
| #36 | Research-doc housekeeping + `measure_resolution.py` | chore | ✅ merged |
| #37 | Explainability — counterfactual recourse + honest precision | feat | ✅ merged |
| #38 | Human oversight (override) + bias-audit disclosure | feat | ✅ merged |
| #43 | `ARCHITECTURE.md` — system study guide + Mermaid diagrams | docs | ✅ merged |
| #44 | Reorder recruiter drill-in (verdict above answers) | feat(ui) | ✅ merged |
| #45 | Demo target ad domain → data-analyst (bridge_fit N/A fix) | fix | ✅ merged |
| #46 | Czech presentation README (primary) + `docs/README.en.md` | docs | ✅ merged |
| #47 | Czech `ARCHITECTURE.cs.md` (1:1 translation) | docs | ✅ merged |
| #48 | `bridge_fit` as a months-to-ready estimate | feat | ✅ merged |
| #49 | `work` evidence tier — real employment no longer weak | fix | ✅ merged |
| #50 | Idempotent `_migrate_columns` — Postgres boot crash fix | fix | ✅ merged |
| #51 | Audit threshold slider (discriminating selection cutoff) | feat | ✅ merged |

---

## Alignment with core assignment

Core assignment: AI matching platform that processes CVs (extraction of
personal data, education, work experience, skills, languages) and matches
candidates to job postings — with specific focus on students /
fresh-graduates / career-changers compared fairly against experienced hires.

### Core pipeline work: ON TARGET

- **#24, #23, #34** — seed/data infrastructure (role-scoped DB, demo rebakes).
  Necessary plumbing, not drift.
- **#26, #27, #21, #29, #50** — Postgres-deploy correctness fixes (sequence
  reset, dialect-agnostic upsert, migration truncate guard, idempotent
  column-add, seed-overlay bug). All keep the deployed pipeline functioning;
  none touch product scope.
- **#30, #31, #33, #45, #49** — classifier and evidence-tier correctness
  fixes (student-vs-experienced-vs-career-changer boundary, real-work-years
  gate, work-evidence mistagged as weak, bridge_fit N/A). This is the core
  "compare fairly against experienced hires" logic — every fix sharpens it.
- **#25, #44, #48** — recruiter UI/UX refinement (form-batched picker,
  drill-in order, bridge_fit as months). On target, cosmetic-to-moderate.
- **#32, #35, #36, #43, #46, #47** — docs (README rewrite + CS translation,
  architecture study guide + CS translation, research reports, housekeeping).
  Docs-only, no behavior change.

### New pillar, not scope creep: compliance/governance surface

**#28, #37, #38, #51** (plus the evidence-tier work in #33/#49) form a
distinct cluster: Model Card, human-oversight override + decision log,
bias-audit (four-fifths ratio) with a tunable threshold, and counterfactual
recourse ("doložit X → coverage N%→M%"). This is driven by the deep-research
queue (`docs/research/#6-8`) grounding it in EU AI Act Art. 14/86 and GDPR
Art. 22 — a defensible, code-grounded response to the platform being a
high-risk hiring-AI system, **not** an unrelated feature addition.

It's flagged here only because it's real: the product now has three pillars
(extraction → matching, **and** governance/audit) rather than two. Worth
naming explicitly in the README/architecture docs as a deliberate pillar
(it already is, informally) so it doesn't read as scope creep to a future
reviewer seeing an "audit" tab on a "CV matcher."

### No drift detected

No PR adds functionality outside the CV→matching→(now)governance surface —
no auth system, no unrelated integrations, no new external services beyond
the already-approved Postgres/Neon path. The single-target MVP model
established in PR #20 is unchanged architecturally.

---

## Flags / concerns

### G1 — ~30-day merge freeze since #51

**Status: worth a status check, not itself a problem**

Last merge was 2026-06-02T14:14:14Z; today is 2026-07-03. Meanwhile **4 PRs
sit open and unmerged**: #39 (Haiku model-tiering), #40 (Haiku A/B harness),
#41 (structured-output path), #42 (prompt-caching) — a stacked branch
implementing the `perf/llm-cost` initiative that CLAUDE.md has listed as
"deferred, planned" since the prior review. All four ship **dark**
(flag-gated off) and are explicitly blocked on the owner running the A/B
harness (#40) with a live key to validate Haiku doesn't regress Czech
extraction before flipping the tier flag.

Recommendation: these are safe to merge as-is (flags default off, zero
behavior change) even before the A/B validation — merging removes 4 branches
worth of drift risk against `main` while keeping the feature dark until
validated. Leaving them open for a month risks a rebase headache with no
offsetting benefit, since merging doesn't require the validation to be done
first.

### G2 — One direct-to-main commit bypassing the branch→PR convention

**Status: minor, content is trivial**

`0c920a8 docs: spinner ~2 LLM volání` (2026-06-02T16:27:11+02:00) is a
1-line UI-copy tweak committed straight to `main`, not via a branch/PR — the
only such commit in this window; everything else in the 26-PR list went
through the convention CLAUDE.md marks as IMPORTANT. Content is harmless
(a spinner label), so no action needed on the change itself — flagging only
because the convention exists precisely to avoid unreviewed main commits,
and a one-off exception is how conventions erode.

### G3 — Recurring pattern: fix-the-previous-PR's-bug

**Status: normal iteration, but density is notable**

#29, #31, #33, #45, #49, #50 are each fixing a regression or gap introduced
by an earlier merge in the *same* window (#28's evidence work needed #33 and
#49 follow-ups; #20's MVP simplification needed #29's overlay fix; the
scraped target ad needed #45's domain fix; #38's new columns needed #50's
idempotency fix). None are severe — all caught and fixed within the window,
several before reaching prod — but six same-window follow-up fixes out of 26
PRs suggests an integration/smoke-test pass across the full candidate→match
flow before merge would catch more of these pre-merge rather than post-merge.

---

## Open deferred items (carried from prior review, status update)

| Item | Priority | Status |
|---|---|---|
| `perf/llm-cost` branch (8→4 LLM calls/CV) | High | **In progress, unmerged** — #39–#42 implement it (Haiku tiering, structured output, prompt caching), shipped dark. See G1. |
| Vendor tool aliases (Power BI, Tableau, Excel → ESCO) | Medium | Still deferred; not touched this window. |
| `personal_fit` retirement cleanup | Low | Not touched this window. |
| README / Mermaid refresh for dual-panel flow | Low | **Done** — #32, #43, #46, #47 (README + ARCHITECTURE, EN+CS, with Mermaid). |
| Czech resolution lift (`measure_resolution.py` first) | Medium | Measurement tool shipped (#36); the actual ESCO-alias-load/embeddings decision still pending the number it produces. |

---

## Current state summary

**Concept: intact.** The core pipeline (extract → resolve into ESCO →
classify candidate type vs. the target ad → score skill coverage → surface
to recruiter) is architecturally unchanged since the single-target MVP
(#20). This window's work is disciplined iteration on that model: correctness
fixes to the classifier and evidence tiers, Postgres-deploy hardening, UX
polish, and full bilingual documentation.

**New direction, deliberately scoped: governance/audit pillar.** #28, #37,
#38, #51 add a Model Card, human-oversight override with a decision log,
bias-audit with a tunable four-fifths-rule threshold, and counterfactual
recourse. This is a genuine expansion of the product's surface area — a
CV-matcher that now also carries compliance tooling — but it's directly
motivated by the platform's own high-risk-AI classification (EU AI Act) and
every feature in the cluster is code-grounded (deterministic, zero extra LLM
calls). Not scope creep against the assignment; worth naming as a third
pillar in top-level docs so it reads as intentional.

**Risks:** a ~30-day merge freeze with 4 flag-gated PRs sitting open (G1,
low severity — safe to merge now, dark), one convention-bypassing direct
commit (G2, trivial content), and a same-window fix-the-prior-fix density
worth addressing with a pre-merge integration smoke pass (G3).

---

## Recommendations

1. **Merge #39–#42 as-is.** They're flag-gated off by default (zero behavior
   change), and merging now avoids further drift against `main`. Run the
   Haiku A/B harness (#40) and flip `CV_BAU_STUDENTS_TIER=1` only after
   confirming Czech-extraction quality holds — that validation gate doesn't
   need to block the merge itself.
2. **Name the governance/audit pillar explicitly** in `README.md` /
   `ARCHITECTURE.md` (e.g. "Extraction → Matching → Oversight & Audit") so
   the Model Card / override / bias-audit surface reads as designed scope,
   not creep, to anyone auditing the repo later (including future review
   passes like this one).
3. **Add one end-to-end smoke test** that runs a full candidate through
   upload → classify → score → recruiter drill-in against the seeded demo
   ad, asserting on shape (doloženost populated, bridge_fit not silently
   N/A, evidence tiers non-empty) — cheap insurance against the class of bug
   G3 describes (a schema/data change in one PR silently breaking a
   downstream reader two PRs later).
4. **Resume `resolve_skill` Czech-alias work** now that `measure_resolution.py`
   (#36) is shipped — it's the one item on the deferred list whose blocking
   prerequisite is now satisfied; run it and let the number decide whether
   ESCO-alias-load or embeddings is warranted.

https://claude.ai/code/session_01CPgv2QH8A3VVcw4CwTmQtZ
