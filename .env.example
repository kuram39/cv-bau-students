ANTHROPIC_API_KEY=your_key_here

# Optional — override the default SQLite file path. Production deploys
# can point this at a Postgres DSN; SQLAlchemy abstracts the dialect.
# CV_BAU_STUDENTS_DB_URL=sqlite:///data/cv_bau_students.sqlite
